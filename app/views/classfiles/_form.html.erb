<%= form_for(@classfile) do |f| %>
  <% if @classfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classfile.errors.count, "error") %> prohibited this classfile from being saved:</h2>

      <ul>
      <% @classfile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
